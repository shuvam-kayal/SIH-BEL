# abis/

Generated, then committed. Run `npm run abis --workspace=bel-contracts`
(wraps `scripts/generate-abis.sh`).

Committed ABIs are what the backend consumes. Reviewing a diff here is
how the team notices an interface change that should have gone through
`docs/CONTRACT_SPEC.md` first — so do not gitignore this directory.
