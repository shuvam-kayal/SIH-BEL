# networking/

Owner: Person 4.

Peer discovery, gossip/broadcast of proposals and votes, and the
message encoding.

The Phase 8 benchmark must measure real message counts here — the
simulator's latency numbers are a placeholder until this exists.
